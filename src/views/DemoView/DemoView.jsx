import React from 'react';
import ElmCarousel from '../../components/Carousel/Carousel';

function DemoView() {
    return (
        <div id='demo'>
            <ElmCarousel />
        </div>
    );

    
}

const container = {
    minWidth: '100%',
    minHeight: '5rm',
};

export default DemoView;
